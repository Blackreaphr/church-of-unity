Place the symbol image file here named exactly:

  symbol.png

Recommended: a square PNG (at least ~512×512). If your file uses a different
extension (e.g., .jpg or .jpeg), either rename it to symbol.png or update the
path in ../index.html accordingly.

Windows path example:
  C:\Users\nicky\Downloads\Church of Unison\assets\symbol.png

